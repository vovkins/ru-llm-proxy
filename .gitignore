# Dependencies
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/
*.egg

# Virtual environments
venv/
.venv/
env/

# Environment & secrets
.env
.env.local
.env.production
# air-gapped runtime env file (some operators keep it as plain `env`, not `.env`)
/env

# Corporate proxy / DLP root CA — never commit the certificate (see certs/README.md)
certs/*.crt
certs/*.pem
certs/*.key

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Docker
docker-compose.override.yml

# NER models (downloaded at setup)
models/
*.bin
*.gguf

# Logs
*.log
logs/

# Data
data/
pgdata/
# HuggingFace / model cache (bind-mounted into presidio-analyzer)
cache/

# Testing
.pytest_cache/
.coverage
htmlcov/
