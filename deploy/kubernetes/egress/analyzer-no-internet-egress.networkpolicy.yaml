apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: ru-llm-proxy-analyzer-no-internet-egress
  namespace: ru-llm-proxy
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: presidio-analyzer
  policyTypes:
    - Egress
  egress: []
