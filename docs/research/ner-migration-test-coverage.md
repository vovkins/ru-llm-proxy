# Покрытие миграции NER проверками

## Назначение

Документ фиксирует проверяемые свойства миграции Analyzer на модель
`fef2/ner_rus_bert-secret_detection` и связывает их с автоматическими
проверками. Матрица нужна для принятия решения по задаче #67: успешный запуск
одного тестового набора не должен подменять проверку качества модели,
корректности интеграции и невозможности передачи чувствительных данных
провайдеру.

Проверки используют только синтетические и обезличенные значения. Действующие
учётные данные и производственные тексты в тестовый набор не входят.

## Матрица покрытия

| Проверяемое свойство | Уровень проверки | Реализация |
| --- | --- | --- |
| Преобразование BIO-меток, объединение WordPiece, точные границы и оценки уверенности | Модульный | `presidio/tests/test_ner.py` |
| Фильтрация типов и применение порога без преждевременного округления | Модульный | `presidio/tests/test_ner.py` |
| NFC-нормализация, эмодзи, мягкий перенос и перевод смещений в исходный текст | Модульный | `presidio/tests/test_ner.py`, `presidio/tests/test_ner_text_processing.py` |
| Перекрывающиеся окна, сущности в хвосте длинного текста и защита от неполного результата | Модульный | `presidio/tests/test_ner.py`, `presidio/tests/test_ner_text_processing.py` |
| Детерминированные правила для учётных данных и договоров, подавление заполнителей | Модульный | `presidio/tests/test_credential_rules.py`, `presidio/tests/test_recognizers.py` |
| Слияние результатов модели и правил с приоритетом специализированных типов | Модульный | `presidio/tests/test_result_merging.py` |
| Запуск, прогрев, состояния готовности, отказ модели, метрики и безопасные события | Модульный/API | `presidio/tests/test_analyzer_server_capacity.py`, `presidio/tests/test_analyzer_api_thresholds.py` |
| Маскирование и блокировка всех восьми типов: `PERSON`, `LOCATION`, `ORGANIZATION`, `LOGIN`, `PASSWORD`, `AUTH_TOKEN`, `SECRET_KEY`, `CONTRACT_NUMBER` | Модульный защитный обработчик | `litellm_guardrails/tests/test_pii_guardrail.py` |
| Восстановление значений в обычном и потоковом ответе | Модульный защитный обработчик | `litellm_guardrails/tests/test_pii_guardrail.py` |
| Маскирование до передачи, восстановление ответа и отсутствие исходных значений у провайдера | Сквозной с реальным Analyzer | `tests/e2e/test_ner_proxy_flow.sh`, `tests/e2e/docker-compose.ner-proxy.yml` |
| Блокировка до передачи и нулевое число обращений к провайдеру | Сквозной с реальным Analyzer | `tests/e2e/test_ner_proxy_flow.sh`, `tests/e2e/docker-compose.ner-proxy.yml` |
| Загрузка закреплённого артефакта, вывод без сети, длинный текст и Unicode | Реальная модель | `presidio/real_model_smoke.py` |
| Качество по обезличенному корпусу и отсутствие регрессии относительно DeepPavlov | Исследовательский корпус | `presidio/tests/test_evaluation.py`, `presidio/evaluation/reports/` |
| Состав образа, закрепление модели и обязательная проверка контрольных сумм | Статический контракт | `tests/test_analyzer_image_contract.py` |
| Состав отдельного CI-контура, кэширование и отсутствие секретов | Статический контракт | `tests/test_ner_integration_config.py` |

## Контуры запуска

### Быстрый обязательный контур

`make test-static`, `make test-recognizers`, `make test-guardrail` и
`make test-flow` проверяют код, контракты конфигурации и основные сценарии без
обращения к внешнему поставщику модели. Процесс GitHub Actions `Baseline CI` выполняет
доступную без тяжёлого образа часть при каждом изменении соответствующего кода.

### Контур с настоящей моделью

`make test-ner-integration` последовательно:

1. собирает производственный образ Analyzer с закреплённой моделью;
2. запускает `real_model_smoke.py` с `--network none`;
3. поднимает LiteLLM, настоящий Analyzer, Redis и тестовый провайдер;
4. проверяет режимы маскирования и блокировки для всех восьми типов;
5. подтверждает отсутствие исходных значений в запросе к провайдеру.

Процесс `NER model integration` использует кэш слоёв GitHub Actions, не требует
секретов поставщика и ограничен 45 минутами. Он запускается при изменении NER,
защитного обработчика или файлов собственного сквозного контура, а также вручную.

### Холодная проверка

Сборка без локального кэша, повторный запуск всего состава контейнеров и анализ
размера образа выполнены как отдельный заключительный этап задачи #67. Этот
этап не подменяется кэшируемым CI-контуром: он проверяет воспроизводимость с
чистого состояния. Условия, измерения и решение `GO` зафиксированы в
[`ner-migration-cold-validation.md`](ner-migration-cold-validation.md).

## Критерии успешности

- все быстрые тестовые наборы завершаются без ошибок;
- реальная модель успешно загружается и выполняет вывод без сети;
- Analyzer сообщает состояние `ready` только после прогрева;
- режим маскирования не передаёт провайдеру исходные значения и полностью
  восстанавливает тестовый ответ;
- режим блокировки возвращает безопасную ошибку до обращения к провайдеру;
- проверены все восемь типов сущностей из области миграции;
- тестовые журналы и отчёты не содержат исходных чувствительных значений;
- проверка качества корпуса остаётся выше согласованных порогов.

## Последний локальный прогон

Проверка от 4 августа 2026 года завершилась со следующими результатами:

| Команда | Результат |
| --- | --- |
| `make test-static` | 136 тестов и 14 проверок целей Makefile пройдены |
| `make test-recognizers` | 352 теста пройдены в контейнере Analyzer |
| `make test-recognizer-api` | 194 теста выделенного API-контура пройдены |
| `make test-guardrail` | 289 тестов защитного обработчика пройдены в контейнере LiteLLM |
| `make test-flow` | 3 детерминированных теста потока пройдены |
| `make test-hf-model-run` | закреплённая модель проверена и успешно запущена без сети |
| `make test-ner-proxy` | маскирование и блокировка всех восьми типов пройдены через настоящий Analyzer |
| `make test-egress-security` | оба сценария запрета передачи провайдеру пройдены |
| `make test-observability-gates` | 4 проверки пройдены |

Дополнительно успешно выполнены проверка YAML, проверка синтаксиса shell,
`compileall` и `git diff --check`. После сквозного теста контейнеров проекта не
осталось.

Холодная сборка без кэша заняла 749,52 с. Полученный образ `linux/arm64`
занимает 3 890 653 145 байт по данным Docker API без учёта разделяемых слоёв.
Сборка подтвердила загрузку CUDA-компонентов для текущего CPU-сценария;
оптимизация размера и профили CPU/GPU относятся к задаче #62. Полные цифры и
ограничения измерений приведены в отчёте о холодной проверке.
